/**
 * WaterSync — RoleSelectionScreen (DEPRECATED)
 * This component is no longer used. Authentication is handled via LoginScreen.
 * Kept as a stub to prevent import errors from any legacy references.
 */
export function RoleSelectionScreen() {
  return null
}
